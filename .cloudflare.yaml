$schema: https://developers.cloudflare.com/pages/platform/build-configuration

# Cloudflare Pages 配置
# 注意：由于项目使用 WebSocket，建议使用 Railway 或 Render

# 如果使用 Cloudflare Pages，需要配置 Functions
# 但 WebSocket 在 Cloudflare 上有限制

# 推荐使用 Railway 或 Render 部署